<div class="enterNames">
  <h2>Player Sign In</h2>
  <form>
    <div class="p1input">
      {{input type="text" value=executioner placeholder="Executioner" cols="60" rows="1"}}
    </div>
    <div class="p2input">
      {{input type="text" value=pname placeholder="Player" cols="60" rows="1"}}
    </div>
  </form>
  {{#link-to "settopic"}}<button type="submit">Submit</button>{{/link-to}}
</div>
